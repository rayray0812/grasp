# Grasp does not require environment variables to run.
#
# AI is optional and user-owned API keys are entered inside the app. Keys are
# stored with platform secure storage; never put them in this file.
